# Aiden Environment Configuration

# Set the environment type (local or dagster)
AIDEN_ENV=local

# Set the working directory for local environment
# Default: ./workdir
AIDEN_WORKDIR=./workdir

# Dagster URL (required for dagster environment)
# DAGSTER_URL=http://localhost:3000

# API keys and other sensitive configuration
# OPENAI_API_KEY=your-api-key-here
